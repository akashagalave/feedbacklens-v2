apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: feedbacklens-monitor
  namespace: feedbacklens
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: gateway
  endpoints:
    - port: "8000"
      path: /metrics
      interval: 15s