apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: feedbacklens
spec:
  selector:
    app: gateway
  ports:
    - port: 8000
      targetPort: 8000
  type: LoadBalancer  #external access , ye hi public endpoint hai