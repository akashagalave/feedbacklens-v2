apiVersion: v1
kind: Service
metadata:
  name: insight-agent
  namespace: feedbacklens
spec:
  selector:
    app: insight-agent
  ports:
    - protocol: TCP
      port: 8003
      targetPort: 8003
  type: ClusterIP