apiVersion: v1
kind: Service
metadata:
  name: recommendation-agent
  namespace: feedbacklens
spec:
  selector:
    app: recommendation-agent
  ports:
    - protocol: TCP
      port: 8004
      targetPort: 8004
  type: ClusterIP